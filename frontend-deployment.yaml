apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-dep
  labels:
    app: react
spec:
  replicas: 3
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
      - name: react
        image: react
        ports:
        - containerPort: 80
